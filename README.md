# Oksskolten AXI

Agent-optimized command-line access to the Oksskolten article archive.

The `oks` CLI will query the authenticated Oksskolten HTTPS API and return
research-ready article results with citation-preserving structured output.

## Status

Under development.
